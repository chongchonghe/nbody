Movies:

- test_small_step_size.mp4: small step sizes, can resolve binaries.
- test_large_step_size_fig.mp4: large step sizes, cannot resolve binaries.
- test_merging.mp4: large step sizes, but a binary-merging mechanism is added to the code, so close encounters are converted to one particle representing the center of mass.
